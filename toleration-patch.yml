spec:
  template:
    spec:
      tolerations:
      - effect: NoSchedule
        key: "hub.jupyter.org/dedicated"
        operator: Equal
        value: user
      - effect: NoSchedule
        key: "hub.jupyter.org_dedicated"
        operator: Equal
        value: "user"
